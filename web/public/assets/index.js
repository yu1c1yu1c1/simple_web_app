const loginBtn = document.getElementsById("loginBtn");

loginBtn.addEventlistner("click", () => {
    

    fetch(url, options)
    .then(response => {
        // レスポンスの処理
    })
    .catch(error => {
    // エラーハンドリング
    });
});
